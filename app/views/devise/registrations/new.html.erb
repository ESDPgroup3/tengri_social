<div class="text-center start_page">
  <h3 class="password_creation m-auto">Создайте пароль</h3>
  <p class="password_explanation text-left m-auto">Придумайте пароль. Пароль должен состоять из 8 знаков на латинице и включать в себя одну цифру</p>
  <div id="phone_error" class="text-info h6 mb-3 d-none">Телефон должен состоять из 10 символов</div>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
  <div class="field">
    <%= f.hidden_field :phone, value: params[:phone] %>
  </div>
  <div class="field border border-info form_for_phone m-auto text-left mb-5">
    <p class="phonenumber text-left m-0">Ваш пароль</p>
    <%= f.password_field :password, autofocus: true, autocomplete: "current-password", :class => "input_phone mr-5" %>
    <span toggle="#user_password" class="fa fa-eye-slash fa-fw field-icon toggle-password d-inline-block mr-0"></span>
  </div>
  <div class="mt-4">
    <div class="field border border-info form_for_phone m-auto text-left">
      <p class="phonenumber text-left m-0">Подтверждение пароля</p>
      <%= f.password_field :password_confirmation, autocomplete: "new-password", :class => "input_phone mr-5"  %>
      <span toggle="#user_password_confirmation" class="fa fa-eye-slash fa-fw field-icon toggle-password d-inline-block mr-0"></span>
    </div>
  </div>
  <%= f.submit 'Продолжить', class: 'send-phone' %>
  <% end %>
</div>