<div class="text-center start_page">
  <h3 class="greeting text-center m-auto">Привет, <%= @user.nickname %></h3>
  <div id="phone_error" class="text-info h6 mb-3 d-none">Телефон должен состоять из 10 символов</div>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.hidden_field :phone, value: params[:phone] %>
  </div>
  <div class="field border border-info form_for_phone m-auto text-left">
    <p class="phonenumber text-left m-0">Ваш пароль</p>
    <%= f.password_field :password, autofocus: true, autocomplete: "current-password", :class => "input_phone" %>
              <a class="border border-success" id="show">
                <i class="fa fa-eye d-none hide-password" aria-hidden="true"></i>
                <i class="fa fa-eye-slash show-password" aria-hidden="true"></i>
              </a>
  </div>
  <%= f.submit 'Продолжить', class: 'send-phone' %>
  <% end %>
    <div class="small-print m-auto">
  <%= link_to "Забыли пароль?", new_password_path(resource_name) %><br />
</div>
</div>