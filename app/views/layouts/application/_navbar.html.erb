<nav class="navbar fixed-top navbar-dark bg-info navbar-expand-md">
  <div class="container">
    <button type="button" class="navbar-toggler collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      &#x2630;
    </button>

    <%= link_to 'Tengri', posts_path, class: 'navbar-brand' %>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <div class="navbar-nav ml-auto">
        <div class="navbar-nav ml-auto">
         <div class="navbar-nav ml-auto">
          <div class="navbar-nav ml-auto">
           <% if user_signed_in? %>
           <div class="dropdown show ">
            <%= link_to current_user.phone , edit_user_registration_path(current_user), class: 'btn table-info dropdown-toggle', 'role' => "button", 'id' => "dropdownMenuLink", 'data-toggle' => "dropdown", 'aria-haspopup' => "true", 'aria-expanded' => "false" %>
            <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
              <%= link_to 'Edit profile', edit_user_registration_path(current_user), class: 'dropdown-item' %>
              <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
            </div>
            <div>
             <% end %>
           </div>
         </div>
       </div>
     </div>
   </nav>

