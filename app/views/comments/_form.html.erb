<%= form_with model: [commentable, Comment.new], local: true do |f|%>
	<%= f.text_area :body, placeholder: "Добавьте коментарий", class: 'form-group' %>
	<%= f.submit class: 'btn btn-secondary' %>

<% end %>